# Sample application for the UART block mode

Just run the Python script provided:

```
python serial_block.py
```

_Note to Windows users_: you might have to change the COM port value to make it work.
